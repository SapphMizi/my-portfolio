---
import { siteConfig } from '../config';

const navLinks = [
  { label: 'Works', href: '/works' },
  { label: 'Blog', href: '/blog' },
  { label: 'About', href: '/#about' },
];
---

<header class="sticky top-0 z-50 border-b border-zinc-800 bg-zinc-950/80 backdrop-blur-sm">
  <nav class="max-w-5xl mx-auto px-4 h-16 flex items-center justify-between">
    <a href="/" class="font-bold text-lg text-white hover:text-violet-400 transition-colors">
      {siteConfig.name}
    </a>
    <ul class="flex gap-6">
      {navLinks.map((link) => (
        <li>
          <a
            href={link.href}
            class="text-zinc-400 hover:text-white text-sm transition-colors"
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>
